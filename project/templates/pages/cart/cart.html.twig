{% extends 'base.html.twig' %}

{% block body %}

    <a href="{{ path('app_home') }}" style="text-decoration: none">⏪ Домой</a>

    <a href="{{ path('app_catalog') }}" style="text-decoration: none">📊 Каталог</a>

    <h1>Корзина</h1>

    {% for cart_item in cart.cart_items ?? [] %}
        <div>
            {{ cart_item.name }} <br>
            {% include 'pages/cart/cart_update.html.twig' with {product: cart_item} %}
        </div>
    {% endfor %}

{% endblock %}
